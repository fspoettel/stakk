{
  "name": "@stakk/stakk",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build --scope=@stakk/api",
    "start": "lerna run start --scope=@stakk/api"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
