{
  "name": "@stakk/stakk",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "lerna run start --scope=@stakk/api --stream",
    "build": "lerna run build --scope@stakk/api --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
