{
  "name": "@stakk/stakk",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start": "lerna run build --scope=@stakk/api --stream && lerna run start --scope=@stakk/api --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
