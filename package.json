{
  "name": "@stakk/stakk",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "postinstall": "lerna run build --scope=@stakk/api --stream",
    "start": "lerna run start --scope=@stakk/api --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
